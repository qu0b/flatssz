// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class BeaconBlockBody {
    public Bytes96 randao_reveal;
    public ETH1Data eth1_data;
    public Bytes32 graffiti;
    public List<ProposerSlashing> proposer_slashings;
    public List<AttesterSlashing> attester_slashings;
    public List<Attestation> attestations;
    public List<Deposit> deposits;
    public List<SignedVoluntaryExit> voluntary_exits;
    public SyncAggregate sync_aggregate;
    public ExecutionPayload execution_payload;
    public List<SignedBLSToExecutionChange> bls_to_execution_changes;
    public List<KZGCommitment> blob_kzg_commitments;

    public BeaconBlockBody() {
        this.randao_reveal = new Bytes96();
        this.eth1_data = new ETH1Data();
        this.graffiti = new Bytes32();
        this.proposer_slashings = new ArrayList<>();
        this.attester_slashings = new ArrayList<>();
        this.attestations = new ArrayList<>();
        this.deposits = new ArrayList<>();
        this.voluntary_exits = new ArrayList<>();
        this.sync_aggregate = new SyncAggregate();
        this.execution_payload = new ExecutionPayload();
        this.bls_to_execution_changes = new ArrayList<>();
        this.blob_kzg_commitments = new ArrayList<>();
    }

    public int sszBytesLen() {
        int size = 392;
        size += this.proposer_slashings.size() * 416;
        size += this.attester_slashings.size() * 4;
        for (AttesterSlashing item : this.attester_slashings) {
            size += item.sszBytesLen();
        }
        size += this.attestations.size() * 4;
        for (Attestation item : this.attestations) {
            size += item.sszBytesLen();
        }
        size += this.deposits.size() * 1240;
        size += this.voluntary_exits.size() * 112;
        size += this.execution_payload.sszBytesLen();
        size += this.bls_to_execution_changes.size() * 172;
        size += this.blob_kzg_commitments.size() * 48;
        return size;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;

        // Field 'randao_reveal'
        this.randao_reveal.marshalSSZTo(buf, pos);
        pos += this.randao_reveal.sszBytesLen();
        // Field 'eth1_data'
        this.eth1_data.marshalSSZTo(buf, pos);
        pos += this.eth1_data.sszBytesLen();
        // Field 'graffiti'
        this.graffiti.marshalSSZTo(buf, pos);
        pos += this.graffiti.sszBytesLen();
        // Offset placeholder for 'proposer_slashings'
        int offsetPos0 = pos;
        pos += 4;
        // Offset placeholder for 'attester_slashings'
        int offsetPos1 = pos;
        pos += 4;
        // Offset placeholder for 'attestations'
        int offsetPos2 = pos;
        pos += 4;
        // Offset placeholder for 'deposits'
        int offsetPos3 = pos;
        pos += 4;
        // Offset placeholder for 'voluntary_exits'
        int offsetPos4 = pos;
        pos += 4;
        // Field 'sync_aggregate'
        this.sync_aggregate.marshalSSZTo(buf, pos);
        pos += this.sync_aggregate.sszBytesLen();
        // Offset placeholder for 'execution_payload'
        int offsetPos5 = pos;
        pos += 4;
        // Offset placeholder for 'bls_to_execution_changes'
        int offsetPos6 = pos;
        pos += 4;
        // Offset placeholder for 'blob_kzg_commitments'
        int offsetPos7 = pos;
        pos += 4;

        // Dynamic field 'proposer_slashings'
        bb.putInt(offsetPos0, pos - offset);
        for (ProposerSlashing item : this.proposer_slashings) {
            item.marshalSSZTo(buf, pos);
            pos += 416;
        }
        // Dynamic field 'attester_slashings'
        bb.putInt(offsetPos1, pos - offset);
        {
            int listStart = pos;
            pos += this.attester_slashings.size() * 4;
            for (int i = 0; i < this.attester_slashings.size(); i++) {
                bb.putInt(listStart + i * 4, pos - listStart);
                this.attester_slashings.get(i).marshalSSZTo(buf, pos);
                pos += this.attester_slashings.get(i).sszBytesLen();
            }
        }
        // Dynamic field 'attestations'
        bb.putInt(offsetPos2, pos - offset);
        {
            int listStart = pos;
            pos += this.attestations.size() * 4;
            for (int i = 0; i < this.attestations.size(); i++) {
                bb.putInt(listStart + i * 4, pos - listStart);
                this.attestations.get(i).marshalSSZTo(buf, pos);
                pos += this.attestations.get(i).sszBytesLen();
            }
        }
        // Dynamic field 'deposits'
        bb.putInt(offsetPos3, pos - offset);
        for (Deposit item : this.deposits) {
            item.marshalSSZTo(buf, pos);
            pos += 1240;
        }
        // Dynamic field 'voluntary_exits'
        bb.putInt(offsetPos4, pos - offset);
        for (SignedVoluntaryExit item : this.voluntary_exits) {
            item.marshalSSZTo(buf, pos);
            pos += 112;
        }
        // Dynamic field 'execution_payload'
        bb.putInt(offsetPos5, pos - offset);
        this.execution_payload.marshalSSZTo(buf, pos);
        pos += this.execution_payload.sszBytesLen();
        // Dynamic field 'bls_to_execution_changes'
        bb.putInt(offsetPos6, pos - offset);
        for (SignedBLSToExecutionChange item : this.bls_to_execution_changes) {
            item.marshalSSZTo(buf, pos);
            pos += 172;
        }
        // Dynamic field 'blob_kzg_commitments'
        bb.putInt(offsetPos7, pos - offset);
        for (KZGCommitment item : this.blob_kzg_commitments) {
            item.marshalSSZTo(buf, pos);
            pos += 48;
        }
    }

    public static BeaconBlockBody fromSSZBytes(byte[] bytes) {
        if (bytes.length < 392) {
            throw new SszError("buffer too small");
        }

        BeaconBlockBody t = new BeaconBlockBody();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        {
            byte[] nested = new byte[96];
            System.arraycopy(bytes, 0, nested, 0, 96);
            t.randao_reveal = Bytes96.fromSSZBytes(nested);
        }
        {
            byte[] nested = new byte[72];
            System.arraycopy(bytes, 96, nested, 0, 72);
            t.eth1_data = ETH1Data.fromSSZBytes(nested);
        }
        {
            byte[] nested = new byte[32];
            System.arraycopy(bytes, 168, nested, 0, 32);
            t.graffiti = Bytes32.fromSSZBytes(nested);
        }
        int offset0 = bb.getInt(200);
        if (offset0 != 392) {
            throw new SszError("invalid offset");
        }
        int offset1 = bb.getInt(204);
        if (offset1 < offset0 || offset1 > bytes.length) {
            throw new SszError("invalid offset");
        }
        int offset2 = bb.getInt(208);
        if (offset2 < offset1 || offset2 > bytes.length) {
            throw new SszError("invalid offset");
        }
        int offset3 = bb.getInt(212);
        if (offset3 < offset2 || offset3 > bytes.length) {
            throw new SszError("invalid offset");
        }
        int offset4 = bb.getInt(216);
        if (offset4 < offset3 || offset4 > bytes.length) {
            throw new SszError("invalid offset");
        }
        {
            byte[] nested = new byte[160];
            System.arraycopy(bytes, 220, nested, 0, 160);
            t.sync_aggregate = SyncAggregate.fromSSZBytes(nested);
        }
        int offset5 = bb.getInt(380);
        if (offset5 < offset4 || offset5 > bytes.length) {
            throw new SszError("invalid offset");
        }
        int offset6 = bb.getInt(384);
        if (offset6 < offset5 || offset6 > bytes.length) {
            throw new SszError("invalid offset");
        }
        int offset7 = bb.getInt(388);
        if (offset7 < offset6 || offset7 > bytes.length) {
            throw new SszError("invalid offset");
        }

        {
            int sliceStart = offset0;
            int sliceEnd = offset1;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            int count = slice.length / 416;
            t.proposer_slashings = new ArrayList<>(count);
            for (int i = 0; i < count; i++) {
                int off = i * 416;
                byte[] elemBytes = new byte[416];
                System.arraycopy(slice, off, elemBytes, 0, 416);
                t.proposer_slashings.add(ProposerSlashing.fromSSZBytes(elemBytes));
            }
        }
        {
            int sliceStart = offset1;
            int sliceEnd = offset2;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            if (slice.length > 0) {
                ByteBuffer sliceBB = ByteBuffer.wrap(slice);
                sliceBB.order(ByteOrder.LITTLE_ENDIAN);
                int firstOff = sliceBB.getInt(0);
                if (firstOff % 4 != 0) {
                    throw new SszError("invalid offset");
                }
                int count = firstOff / 4;
                t.attester_slashings = new ArrayList<>(count);
                for (int i = 0; i < count; i++) {
                    int s = sliceBB.getInt(i * 4);
                    int e = (i + 1 < count) ? sliceBB.getInt((i + 1) * 4) : slice.length;
                    if (s > e || e > slice.length) {
                        throw new SszError("invalid offset");
                    }
                    byte[] elemBytes = new byte[e - s];
                    System.arraycopy(slice, s, elemBytes, 0, e - s);
                    t.attester_slashings.add(AttesterSlashing.fromSSZBytes(elemBytes));
                }
            }
        }
        {
            int sliceStart = offset2;
            int sliceEnd = offset3;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            if (slice.length > 0) {
                ByteBuffer sliceBB = ByteBuffer.wrap(slice);
                sliceBB.order(ByteOrder.LITTLE_ENDIAN);
                int firstOff = sliceBB.getInt(0);
                if (firstOff % 4 != 0) {
                    throw new SszError("invalid offset");
                }
                int count = firstOff / 4;
                t.attestations = new ArrayList<>(count);
                for (int i = 0; i < count; i++) {
                    int s = sliceBB.getInt(i * 4);
                    int e = (i + 1 < count) ? sliceBB.getInt((i + 1) * 4) : slice.length;
                    if (s > e || e > slice.length) {
                        throw new SszError("invalid offset");
                    }
                    byte[] elemBytes = new byte[e - s];
                    System.arraycopy(slice, s, elemBytes, 0, e - s);
                    t.attestations.add(Attestation.fromSSZBytes(elemBytes));
                }
            }
        }
        {
            int sliceStart = offset3;
            int sliceEnd = offset4;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            int count = slice.length / 1240;
            t.deposits = new ArrayList<>(count);
            for (int i = 0; i < count; i++) {
                int off = i * 1240;
                byte[] elemBytes = new byte[1240];
                System.arraycopy(slice, off, elemBytes, 0, 1240);
                t.deposits.add(Deposit.fromSSZBytes(elemBytes));
            }
        }
        {
            int sliceStart = offset4;
            int sliceEnd = offset5;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            int count = slice.length / 112;
            t.voluntary_exits = new ArrayList<>(count);
            for (int i = 0; i < count; i++) {
                int off = i * 112;
                byte[] elemBytes = new byte[112];
                System.arraycopy(slice, off, elemBytes, 0, 112);
                t.voluntary_exits.add(SignedVoluntaryExit.fromSSZBytes(elemBytes));
            }
        }
        {
            int sliceStart = offset5;
            int sliceEnd = offset6;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            t.execution_payload = ExecutionPayload.fromSSZBytes(slice);
        }
        {
            int sliceStart = offset6;
            int sliceEnd = offset7;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            int count = slice.length / 172;
            t.bls_to_execution_changes = new ArrayList<>(count);
            for (int i = 0; i < count; i++) {
                int off = i * 172;
                byte[] elemBytes = new byte[172];
                System.arraycopy(slice, off, elemBytes, 0, 172);
                t.bls_to_execution_changes.add(SignedBLSToExecutionChange.fromSSZBytes(elemBytes));
            }
        }
        {
            int sliceStart = offset7;
            int sliceEnd = bytes.length;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            int count = slice.length / 48;
            t.blob_kzg_commitments = new ArrayList<>(count);
            for (int i = 0; i < count; i++) {
                int off = i * 48;
                byte[] elemBytes = new byte[48];
                System.arraycopy(slice, off, elemBytes, 0, 48);
                t.blob_kzg_commitments.add(KZGCommitment.fromSSZBytes(elemBytes));
            }
        }
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'randao_reveal'
        this.randao_reveal.hashTreeRootWith(hh);

        // Field 'eth1_data'
        this.eth1_data.hashTreeRootWith(hh);

        // Field 'graffiti'
        this.graffiti.hashTreeRootWith(hh);

        // Field 'proposer_slashings'
        {
            int subIdx = hh.index();
            for (ProposerSlashing item : this.proposer_slashings) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.proposer_slashings.size(), 16);
        }

        // Field 'attester_slashings'
        {
            int subIdx = hh.index();
            for (AttesterSlashing item : this.attester_slashings) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.attester_slashings.size(), 2);
        }

        // Field 'attestations'
        {
            int subIdx = hh.index();
            for (Attestation item : this.attestations) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.attestations.size(), 128);
        }

        // Field 'deposits'
        {
            int subIdx = hh.index();
            for (Deposit item : this.deposits) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.deposits.size(), 16);
        }

        // Field 'voluntary_exits'
        {
            int subIdx = hh.index();
            for (SignedVoluntaryExit item : this.voluntary_exits) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.voluntary_exits.size(), 16);
        }

        // Field 'sync_aggregate'
        this.sync_aggregate.hashTreeRootWith(hh);

        // Field 'execution_payload'
        this.execution_payload.hashTreeRootWith(hh);

        // Field 'bls_to_execution_changes'
        {
            int subIdx = hh.index();
            for (SignedBLSToExecutionChange item : this.bls_to_execution_changes) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.bls_to_execution_changes.size(), 16);
        }

        // Field 'blob_kzg_commitments'
        {
            int subIdx = hh.index();
            for (KZGCommitment item : this.blob_kzg_commitments) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.blob_kzg_commitments.size(), 4096);
        }

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'randao_reveal'
        this.randao_reveal.hashTreeRootWith(hh);

        // Field 'eth1_data'
        this.eth1_data.hashTreeRootWith(hh);

        // Field 'graffiti'
        this.graffiti.hashTreeRootWith(hh);

        // Field 'proposer_slashings'
        {
            int subIdx = hh.index();
            for (ProposerSlashing item : this.proposer_slashings) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.proposer_slashings.size(), 16);
        }

        // Field 'attester_slashings'
        {
            int subIdx = hh.index();
            for (AttesterSlashing item : this.attester_slashings) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.attester_slashings.size(), 2);
        }

        // Field 'attestations'
        {
            int subIdx = hh.index();
            for (Attestation item : this.attestations) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.attestations.size(), 128);
        }

        // Field 'deposits'
        {
            int subIdx = hh.index();
            for (Deposit item : this.deposits) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.deposits.size(), 16);
        }

        // Field 'voluntary_exits'
        {
            int subIdx = hh.index();
            for (SignedVoluntaryExit item : this.voluntary_exits) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.voluntary_exits.size(), 16);
        }

        // Field 'sync_aggregate'
        this.sync_aggregate.hashTreeRootWith(hh);

        // Field 'execution_payload'
        this.execution_payload.hashTreeRootWith(hh);

        // Field 'bls_to_execution_changes'
        {
            int subIdx = hh.index();
            for (SignedBLSToExecutionChange item : this.bls_to_execution_changes) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.bls_to_execution_changes.size(), 16);
        }

        // Field 'blob_kzg_commitments'
        {
            int subIdx = hh.index();
            for (KZGCommitment item : this.blob_kzg_commitments) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.blob_kzg_commitments.size(), 4096);
        }

        hh.merkleize(idx);
    }
}
