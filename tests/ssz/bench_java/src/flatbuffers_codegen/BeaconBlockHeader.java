// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class BeaconBlockHeader {
    public long slot;
    public long proposer_index;
    public byte[] parent_root;
    public byte[] state_root;
    public byte[] body_root;

    public BeaconBlockHeader() {
        this.parent_root = new byte[32];
        this.state_root = new byte[32];
        this.body_root = new byte[32];
    }

    public int sszBytesLen() {
        return 112;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;
        bb.putLong(pos, this.slot);
        pos += 8;
        bb.putLong(pos, this.proposer_index);
        pos += 8;
        System.arraycopy(this.parent_root, 0, buf, pos, 32);
        pos += 32;
        System.arraycopy(this.state_root, 0, buf, pos, 32);
        pos += 32;
        System.arraycopy(this.body_root, 0, buf, pos, 32);
        pos += 32;
    }

    public static BeaconBlockHeader fromSSZBytes(byte[] bytes) {
        if (bytes.length < 112) {
            throw new SszError("buffer too small");
        }

        BeaconBlockHeader t = new BeaconBlockHeader();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        t.slot = bb.getLong(0);
        t.proposer_index = bb.getLong(8);
        t.parent_root = new byte[32];
        System.arraycopy(bytes, 16, t.parent_root, 0, 32);
        t.state_root = new byte[32];
        System.arraycopy(bytes, 48, t.state_root, 0, 32);
        t.body_root = new byte[32];
        System.arraycopy(bytes, 80, t.body_root, 0, 32);
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'slot'
        hh.putUint64(this.slot);

        // Field 'proposer_index'
        hh.putUint64(this.proposer_index);

        // Field 'parent_root'
        hh.putBytes(this.parent_root);

        // Field 'state_root'
        hh.putBytes(this.state_root);

        // Field 'body_root'
        hh.putBytes(this.body_root);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'slot'
        hh.putUint64(this.slot);

        // Field 'proposer_index'
        hh.putUint64(this.proposer_index);

        // Field 'parent_root'
        hh.putBytes(this.parent_root);

        // Field 'state_root'
        hh.putBytes(this.state_root);

        // Field 'body_root'
        hh.putBytes(this.body_root);

        hh.merkleize(idx);
    }
}
