// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class Bytes20 {
    public byte[] data;

    public Bytes20() {
        this.data = new byte[20];
    }

    public int sszBytesLen() {
        return 20;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;
        System.arraycopy(this.data, 0, buf, pos, 20);
        pos += 20;
    }

    public static Bytes20 fromSSZBytes(byte[] bytes) {
        if (bytes.length < 20) {
            throw new SszError("buffer too small");
        }

        Bytes20 t = new Bytes20();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        t.data = new byte[20];
        System.arraycopy(bytes, 0, t.data, 0, 20);
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'data'
        hh.putBytes(this.data);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'data'
        hh.putBytes(this.data);

        hh.merkleize(idx);
    }
}
