// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class Checkpoint {
    public long epoch;
    public byte[] root;

    public Checkpoint() {
        this.root = new byte[32];
    }

    public int sszBytesLen() {
        return 40;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;
        bb.putLong(pos, this.epoch);
        pos += 8;
        System.arraycopy(this.root, 0, buf, pos, 32);
        pos += 32;
    }

    public static Checkpoint fromSSZBytes(byte[] bytes) {
        if (bytes.length < 40) {
            throw new SszError("buffer too small");
        }

        Checkpoint t = new Checkpoint();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        t.epoch = bb.getLong(0);
        t.root = new byte[32];
        System.arraycopy(bytes, 8, t.root, 0, 32);
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'epoch'
        hh.putUint64(this.epoch);

        // Field 'root'
        hh.putBytes(this.root);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'epoch'
        hh.putUint64(this.epoch);

        // Field 'root'
        hh.putBytes(this.root);

        hh.merkleize(idx);
    }
}
