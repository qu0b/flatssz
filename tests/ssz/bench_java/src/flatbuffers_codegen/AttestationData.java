// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class AttestationData {
    public long slot;
    public long index;
    public byte[] beacon_block_root;
    public Checkpoint source;
    public Checkpoint target;

    public AttestationData() {
        this.beacon_block_root = new byte[32];
        this.source = new Checkpoint();
        this.target = new Checkpoint();
    }

    public int sszBytesLen() {
        return 128;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;
        bb.putLong(pos, this.slot);
        pos += 8;
        bb.putLong(pos, this.index);
        pos += 8;
        System.arraycopy(this.beacon_block_root, 0, buf, pos, 32);
        pos += 32;
        this.source.marshalSSZTo(buf, pos);
        pos += this.source.sszBytesLen();
        this.target.marshalSSZTo(buf, pos);
        pos += this.target.sszBytesLen();
    }

    public static AttestationData fromSSZBytes(byte[] bytes) {
        if (bytes.length < 128) {
            throw new SszError("buffer too small");
        }

        AttestationData t = new AttestationData();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        t.slot = bb.getLong(0);
        t.index = bb.getLong(8);
        t.beacon_block_root = new byte[32];
        System.arraycopy(bytes, 16, t.beacon_block_root, 0, 32);
        {
            byte[] nested = new byte[40];
            System.arraycopy(bytes, 48, nested, 0, 40);
            t.source = Checkpoint.fromSSZBytes(nested);
        }
        {
            byte[] nested = new byte[40];
            System.arraycopy(bytes, 88, nested, 0, 40);
            t.target = Checkpoint.fromSSZBytes(nested);
        }
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'slot'
        hh.putUint64(this.slot);

        // Field 'index'
        hh.putUint64(this.index);

        // Field 'beacon_block_root'
        hh.putBytes(this.beacon_block_root);

        // Field 'source'
        this.source.hashTreeRootWith(hh);

        // Field 'target'
        this.target.hashTreeRootWith(hh);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'slot'
        hh.putUint64(this.slot);

        // Field 'index'
        hh.putUint64(this.index);

        // Field 'beacon_block_root'
        hh.putBytes(this.beacon_block_root);

        // Field 'source'
        this.source.hashTreeRootWith(hh);

        // Field 'target'
        this.target.hashTreeRootWith(hh);

        hh.merkleize(idx);
    }
}
