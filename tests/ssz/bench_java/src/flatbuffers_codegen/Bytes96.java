// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class Bytes96 {
    public byte[] data;

    public Bytes96() {
        this.data = new byte[96];
    }

    public int sszBytesLen() {
        return 96;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;
        System.arraycopy(this.data, 0, buf, pos, 96);
        pos += 96;
    }

    public static Bytes96 fromSSZBytes(byte[] bytes) {
        if (bytes.length < 96) {
            throw new SszError("buffer too small");
        }

        Bytes96 t = new Bytes96();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        t.data = new byte[96];
        System.arraycopy(bytes, 0, t.data, 0, 96);
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'data'
        hh.putBytes(this.data);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'data'
        hh.putBytes(this.data);

        hh.merkleize(idx);
    }
}
