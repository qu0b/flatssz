// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class ETH1Data {
    public byte[] deposit_root;
    public long deposit_count;
    public byte[] block_hash;

    public ETH1Data() {
        this.deposit_root = new byte[32];
        this.block_hash = new byte[32];
    }

    public int sszBytesLen() {
        return 72;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;
        System.arraycopy(this.deposit_root, 0, buf, pos, 32);
        pos += 32;
        bb.putLong(pos, this.deposit_count);
        pos += 8;
        System.arraycopy(this.block_hash, 0, buf, pos, 32);
        pos += 32;
    }

    public static ETH1Data fromSSZBytes(byte[] bytes) {
        if (bytes.length < 72) {
            throw new SszError("buffer too small");
        }

        ETH1Data t = new ETH1Data();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        t.deposit_root = new byte[32];
        System.arraycopy(bytes, 0, t.deposit_root, 0, 32);
        t.deposit_count = bb.getLong(32);
        t.block_hash = new byte[32];
        System.arraycopy(bytes, 40, t.block_hash, 0, 32);
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'deposit_root'
        hh.putBytes(this.deposit_root);

        // Field 'deposit_count'
        hh.putUint64(this.deposit_count);

        // Field 'block_hash'
        hh.putBytes(this.block_hash);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'deposit_root'
        hh.putBytes(this.deposit_root);

        // Field 'deposit_count'
        hh.putUint64(this.deposit_count);

        // Field 'block_hash'
        hh.putBytes(this.block_hash);

        hh.merkleize(idx);
    }
}
