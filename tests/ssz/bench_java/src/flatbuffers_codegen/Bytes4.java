// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class Bytes4 {
    public byte[] data;

    public Bytes4() {
        this.data = new byte[4];
    }

    public int sszBytesLen() {
        return 4;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;
        System.arraycopy(this.data, 0, buf, pos, 4);
        pos += 4;
    }

    public static Bytes4 fromSSZBytes(byte[] bytes) {
        if (bytes.length < 4) {
            throw new SszError("buffer too small");
        }

        Bytes4 t = new Bytes4();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        t.data = new byte[4];
        System.arraycopy(bytes, 0, t.data, 0, 4);
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'data'
        hh.putBytes(this.data);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'data'
        hh.putBytes(this.data);

        hh.merkleize(idx);
    }
}
