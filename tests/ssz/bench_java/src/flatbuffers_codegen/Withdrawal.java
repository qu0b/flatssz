// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class Withdrawal {
    public long index;
    public long validator_index;
    public byte[] address;
    public long amount;

    public Withdrawal() {
        this.address = new byte[20];
    }

    public int sszBytesLen() {
        return 44;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;
        bb.putLong(pos, this.index);
        pos += 8;
        bb.putLong(pos, this.validator_index);
        pos += 8;
        System.arraycopy(this.address, 0, buf, pos, 20);
        pos += 20;
        bb.putLong(pos, this.amount);
        pos += 8;
    }

    public static Withdrawal fromSSZBytes(byte[] bytes) {
        if (bytes.length < 44) {
            throw new SszError("buffer too small");
        }

        Withdrawal t = new Withdrawal();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        t.index = bb.getLong(0);
        t.validator_index = bb.getLong(8);
        t.address = new byte[20];
        System.arraycopy(bytes, 16, t.address, 0, 20);
        t.amount = bb.getLong(36);
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'index'
        hh.putUint64(this.index);

        // Field 'validator_index'
        hh.putUint64(this.validator_index);

        // Field 'address'
        hh.putBytes(this.address);

        // Field 'amount'
        hh.putUint64(this.amount);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'index'
        hh.putUint64(this.index);

        // Field 'validator_index'
        hh.putUint64(this.validator_index);

        // Field 'address'
        hh.putBytes(this.address);

        // Field 'amount'
        hh.putUint64(this.amount);

        hh.merkleize(idx);
    }
}
