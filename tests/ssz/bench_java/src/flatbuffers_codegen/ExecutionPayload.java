// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class ExecutionPayload {
    public Bytes32 parent_hash;
    public Bytes20 fee_recipient;
    public Bytes32 state_root;
    public Bytes32 receipts_root;
    public Bytes256 logs_bloom;
    public Bytes32 prev_randao;
    public long block_number;
    public long gas_limit;
    public long gas_used;
    public long timestamp;
    public byte[] extra_data;
    public Bytes32 base_fee_per_gas;
    public Bytes32 block_hash;
    public List<byte[]> transactions;
    public List<Withdrawal> withdrawals;
    public long blob_gas_used;
    public long excess_blob_gas;

    public ExecutionPayload() {
        this.parent_hash = new Bytes32();
        this.fee_recipient = new Bytes20();
        this.state_root = new Bytes32();
        this.receipts_root = new Bytes32();
        this.logs_bloom = new Bytes256();
        this.prev_randao = new Bytes32();
        this.extra_data = new byte[0];
        this.base_fee_per_gas = new Bytes32();
        this.block_hash = new Bytes32();
        this.transactions = new ArrayList<>();
        this.withdrawals = new ArrayList<>();
    }

    public int sszBytesLen() {
        int size = 528;
        size += this.extra_data.length;
        size += this.transactions.size() * 4;
        for (byte[] item : this.transactions) {
            size += item.length;
        }
        size += this.withdrawals.size() * 44;
        return size;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;

        // Field 'parent_hash'
        this.parent_hash.marshalSSZTo(buf, pos);
        pos += this.parent_hash.sszBytesLen();
        // Field 'fee_recipient'
        this.fee_recipient.marshalSSZTo(buf, pos);
        pos += this.fee_recipient.sszBytesLen();
        // Field 'state_root'
        this.state_root.marshalSSZTo(buf, pos);
        pos += this.state_root.sszBytesLen();
        // Field 'receipts_root'
        this.receipts_root.marshalSSZTo(buf, pos);
        pos += this.receipts_root.sszBytesLen();
        // Field 'logs_bloom'
        this.logs_bloom.marshalSSZTo(buf, pos);
        pos += this.logs_bloom.sszBytesLen();
        // Field 'prev_randao'
        this.prev_randao.marshalSSZTo(buf, pos);
        pos += this.prev_randao.sszBytesLen();
        // Field 'block_number'
        bb.putLong(pos, this.block_number);
        pos += 8;
        // Field 'gas_limit'
        bb.putLong(pos, this.gas_limit);
        pos += 8;
        // Field 'gas_used'
        bb.putLong(pos, this.gas_used);
        pos += 8;
        // Field 'timestamp'
        bb.putLong(pos, this.timestamp);
        pos += 8;
        // Offset placeholder for 'extra_data'
        int offsetPos0 = pos;
        pos += 4;
        // Field 'base_fee_per_gas'
        this.base_fee_per_gas.marshalSSZTo(buf, pos);
        pos += this.base_fee_per_gas.sszBytesLen();
        // Field 'block_hash'
        this.block_hash.marshalSSZTo(buf, pos);
        pos += this.block_hash.sszBytesLen();
        // Offset placeholder for 'transactions'
        int offsetPos1 = pos;
        pos += 4;
        // Offset placeholder for 'withdrawals'
        int offsetPos2 = pos;
        pos += 4;
        // Field 'blob_gas_used'
        bb.putLong(pos, this.blob_gas_used);
        pos += 8;
        // Field 'excess_blob_gas'
        bb.putLong(pos, this.excess_blob_gas);
        pos += 8;

        // Dynamic field 'extra_data'
        bb.putInt(offsetPos0, pos - offset);
        System.arraycopy(this.extra_data, 0, buf, pos, this.extra_data.length);
        pos += this.extra_data.length;
        // Dynamic field 'transactions'
        bb.putInt(offsetPos1, pos - offset);
        {
            int listStart = pos;
            pos += this.transactions.size() * 4;
            for (int i = 0; i < this.transactions.size(); i++) {
                bb.putInt(listStart + i * 4, pos - listStart);
                byte[] item = this.transactions.get(i);
                System.arraycopy(item, 0, buf, pos, item.length);
                pos += item.length;
            }
        }
        // Dynamic field 'withdrawals'
        bb.putInt(offsetPos2, pos - offset);
        for (Withdrawal item : this.withdrawals) {
            item.marshalSSZTo(buf, pos);
            pos += 44;
        }
    }

    public static ExecutionPayload fromSSZBytes(byte[] bytes) {
        if (bytes.length < 528) {
            throw new SszError("buffer too small");
        }

        ExecutionPayload t = new ExecutionPayload();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        {
            byte[] nested = new byte[32];
            System.arraycopy(bytes, 0, nested, 0, 32);
            t.parent_hash = Bytes32.fromSSZBytes(nested);
        }
        {
            byte[] nested = new byte[20];
            System.arraycopy(bytes, 32, nested, 0, 20);
            t.fee_recipient = Bytes20.fromSSZBytes(nested);
        }
        {
            byte[] nested = new byte[32];
            System.arraycopy(bytes, 52, nested, 0, 32);
            t.state_root = Bytes32.fromSSZBytes(nested);
        }
        {
            byte[] nested = new byte[32];
            System.arraycopy(bytes, 84, nested, 0, 32);
            t.receipts_root = Bytes32.fromSSZBytes(nested);
        }
        {
            byte[] nested = new byte[256];
            System.arraycopy(bytes, 116, nested, 0, 256);
            t.logs_bloom = Bytes256.fromSSZBytes(nested);
        }
        {
            byte[] nested = new byte[32];
            System.arraycopy(bytes, 372, nested, 0, 32);
            t.prev_randao = Bytes32.fromSSZBytes(nested);
        }
        t.block_number = bb.getLong(404);
        t.gas_limit = bb.getLong(412);
        t.gas_used = bb.getLong(420);
        t.timestamp = bb.getLong(428);
        int offset0 = bb.getInt(436);
        if (offset0 != 528) {
            throw new SszError("invalid offset");
        }
        {
            byte[] nested = new byte[32];
            System.arraycopy(bytes, 440, nested, 0, 32);
            t.base_fee_per_gas = Bytes32.fromSSZBytes(nested);
        }
        {
            byte[] nested = new byte[32];
            System.arraycopy(bytes, 472, nested, 0, 32);
            t.block_hash = Bytes32.fromSSZBytes(nested);
        }
        int offset1 = bb.getInt(504);
        if (offset1 < offset0 || offset1 > bytes.length) {
            throw new SszError("invalid offset");
        }
        int offset2 = bb.getInt(508);
        if (offset2 < offset1 || offset2 > bytes.length) {
            throw new SszError("invalid offset");
        }
        t.blob_gas_used = bb.getLong(512);
        t.excess_blob_gas = bb.getLong(520);

        {
            int sliceStart = offset0;
            int sliceEnd = offset1;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            t.extra_data = new byte[slice.length];
            System.arraycopy(slice, 0, t.extra_data, 0, slice.length);
        }
        {
            int sliceStart = offset1;
            int sliceEnd = offset2;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            if (slice.length > 0) {
                ByteBuffer sliceBB = ByteBuffer.wrap(slice);
                sliceBB.order(ByteOrder.LITTLE_ENDIAN);
                int firstOff = sliceBB.getInt(0);
                if (firstOff % 4 != 0) {
                    throw new SszError("invalid offset");
                }
                int count = firstOff / 4;
                t.transactions = new ArrayList<>(count);
                for (int i = 0; i < count; i++) {
                    int s = sliceBB.getInt(i * 4);
                    int e = (i + 1 < count) ? sliceBB.getInt((i + 1) * 4) : slice.length;
                    if (s > e || e > slice.length) {
                        throw new SszError("invalid offset");
                    }
                    byte[] elem = new byte[e - s];
                    System.arraycopy(slice, s, elem, 0, e - s);
                    t.transactions.add(elem);
                }
            }
        }
        {
            int sliceStart = offset2;
            int sliceEnd = bytes.length;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            int count = slice.length / 44;
            t.withdrawals = new ArrayList<>(count);
            for (int i = 0; i < count; i++) {
                int off = i * 44;
                byte[] elemBytes = new byte[44];
                System.arraycopy(slice, off, elemBytes, 0, 44);
                t.withdrawals.add(Withdrawal.fromSSZBytes(elemBytes));
            }
        }
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'parent_hash'
        this.parent_hash.hashTreeRootWith(hh);

        // Field 'fee_recipient'
        this.fee_recipient.hashTreeRootWith(hh);

        // Field 'state_root'
        this.state_root.hashTreeRootWith(hh);

        // Field 'receipts_root'
        this.receipts_root.hashTreeRootWith(hh);

        // Field 'logs_bloom'
        this.logs_bloom.hashTreeRootWith(hh);

        // Field 'prev_randao'
        this.prev_randao.hashTreeRootWith(hh);

        // Field 'block_number'
        hh.putUint64(this.block_number);

        // Field 'gas_limit'
        hh.putUint64(this.gas_limit);

        // Field 'gas_used'
        hh.putUint64(this.gas_used);

        // Field 'timestamp'
        hh.putUint64(this.timestamp);

        // Field 'extra_data'
        {
            int subIdx = hh.index();
            hh.appendBytes32(this.extra_data);
            hh.merkleizeWithMixin(subIdx, this.extra_data.length, 1);
        }

        // Field 'base_fee_per_gas'
        this.base_fee_per_gas.hashTreeRootWith(hh);

        // Field 'block_hash'
        this.block_hash.hashTreeRootWith(hh);

        // Field 'transactions'
        {
            int subIdx = hh.index();
            for (byte[] item : this.transactions) {
                int inner = hh.index();
                hh.appendBytes32(item);
                hh.merkleizeWithMixin(inner, item.length, 33554432);
            }
            hh.merkleizeWithMixin(subIdx, this.transactions.size(), 1048576);
        }

        // Field 'withdrawals'
        {
            int subIdx = hh.index();
            for (Withdrawal item : this.withdrawals) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.withdrawals.size(), 16);
        }

        // Field 'blob_gas_used'
        hh.putUint64(this.blob_gas_used);

        // Field 'excess_blob_gas'
        hh.putUint64(this.excess_blob_gas);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'parent_hash'
        this.parent_hash.hashTreeRootWith(hh);

        // Field 'fee_recipient'
        this.fee_recipient.hashTreeRootWith(hh);

        // Field 'state_root'
        this.state_root.hashTreeRootWith(hh);

        // Field 'receipts_root'
        this.receipts_root.hashTreeRootWith(hh);

        // Field 'logs_bloom'
        this.logs_bloom.hashTreeRootWith(hh);

        // Field 'prev_randao'
        this.prev_randao.hashTreeRootWith(hh);

        // Field 'block_number'
        hh.putUint64(this.block_number);

        // Field 'gas_limit'
        hh.putUint64(this.gas_limit);

        // Field 'gas_used'
        hh.putUint64(this.gas_used);

        // Field 'timestamp'
        hh.putUint64(this.timestamp);

        // Field 'extra_data'
        {
            int subIdx = hh.index();
            hh.appendBytes32(this.extra_data);
            hh.merkleizeWithMixin(subIdx, this.extra_data.length, 1);
        }

        // Field 'base_fee_per_gas'
        this.base_fee_per_gas.hashTreeRootWith(hh);

        // Field 'block_hash'
        this.block_hash.hashTreeRootWith(hh);

        // Field 'transactions'
        {
            int subIdx = hh.index();
            for (byte[] item : this.transactions) {
                int inner = hh.index();
                hh.appendBytes32(item);
                hh.merkleizeWithMixin(inner, item.length, 33554432);
            }
            hh.merkleizeWithMixin(subIdx, this.transactions.size(), 1048576);
        }

        // Field 'withdrawals'
        {
            int subIdx = hh.index();
            for (Withdrawal item : this.withdrawals) {
                item.hashTreeRootWith(hh);
            }
            hh.merkleizeWithMixin(subIdx, this.withdrawals.size(), 16);
        }

        // Field 'blob_gas_used'
        hh.putUint64(this.blob_gas_used);

        // Field 'excess_blob_gas'
        hh.putUint64(this.excess_blob_gas);

        hh.merkleize(idx);
    }
}
