// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class BLSToExecutionChange {
    public long validator_index;
    public byte[] from_bls_pubkey;
    public byte[] to_execution_address;

    public BLSToExecutionChange() {
        this.from_bls_pubkey = new byte[48];
        this.to_execution_address = new byte[20];
    }

    public int sszBytesLen() {
        return 76;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;
        bb.putLong(pos, this.validator_index);
        pos += 8;
        System.arraycopy(this.from_bls_pubkey, 0, buf, pos, 48);
        pos += 48;
        System.arraycopy(this.to_execution_address, 0, buf, pos, 20);
        pos += 20;
    }

    public static BLSToExecutionChange fromSSZBytes(byte[] bytes) {
        if (bytes.length < 76) {
            throw new SszError("buffer too small");
        }

        BLSToExecutionChange t = new BLSToExecutionChange();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        t.validator_index = bb.getLong(0);
        t.from_bls_pubkey = new byte[48];
        System.arraycopy(bytes, 8, t.from_bls_pubkey, 0, 48);
        t.to_execution_address = new byte[20];
        System.arraycopy(bytes, 56, t.to_execution_address, 0, 20);
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'validator_index'
        hh.putUint64(this.validator_index);

        // Field 'from_bls_pubkey'
        hh.putBytes(this.from_bls_pubkey);

        // Field 'to_execution_address'
        hh.putBytes(this.to_execution_address);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'validator_index'
        hh.putUint64(this.validator_index);

        // Field 'from_bls_pubkey'
        hh.putBytes(this.from_bls_pubkey);

        // Field 'to_execution_address'
        hh.putBytes(this.to_execution_address);

        hh.merkleize(idx);
    }
}
