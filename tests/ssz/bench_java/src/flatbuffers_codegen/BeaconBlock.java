// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class BeaconBlock {
    public long slot;
    public long proposer_index;
    public Bytes32 parent_root;
    public Bytes32 state_root;
    public BeaconBlockBody body;

    public BeaconBlock() {
        this.parent_root = new Bytes32();
        this.state_root = new Bytes32();
        this.body = new BeaconBlockBody();
    }

    public int sszBytesLen() {
        int size = 84;
        size += this.body.sszBytesLen();
        return size;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;

        // Field 'slot'
        bb.putLong(pos, this.slot);
        pos += 8;
        // Field 'proposer_index'
        bb.putLong(pos, this.proposer_index);
        pos += 8;
        // Field 'parent_root'
        this.parent_root.marshalSSZTo(buf, pos);
        pos += this.parent_root.sszBytesLen();
        // Field 'state_root'
        this.state_root.marshalSSZTo(buf, pos);
        pos += this.state_root.sszBytesLen();
        // Offset placeholder for 'body'
        int offsetPos0 = pos;
        pos += 4;

        // Dynamic field 'body'
        bb.putInt(offsetPos0, pos - offset);
        this.body.marshalSSZTo(buf, pos);
        pos += this.body.sszBytesLen();
    }

    public static BeaconBlock fromSSZBytes(byte[] bytes) {
        if (bytes.length < 84) {
            throw new SszError("buffer too small");
        }

        BeaconBlock t = new BeaconBlock();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        t.slot = bb.getLong(0);
        t.proposer_index = bb.getLong(8);
        {
            byte[] nested = new byte[32];
            System.arraycopy(bytes, 16, nested, 0, 32);
            t.parent_root = Bytes32.fromSSZBytes(nested);
        }
        {
            byte[] nested = new byte[32];
            System.arraycopy(bytes, 48, nested, 0, 32);
            t.state_root = Bytes32.fromSSZBytes(nested);
        }
        int offset0 = bb.getInt(80);
        if (offset0 != 84) {
            throw new SszError("invalid offset");
        }

        {
            int sliceStart = offset0;
            int sliceEnd = bytes.length;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            t.body = BeaconBlockBody.fromSSZBytes(slice);
        }
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'slot'
        hh.putUint64(this.slot);

        // Field 'proposer_index'
        hh.putUint64(this.proposer_index);

        // Field 'parent_root'
        this.parent_root.hashTreeRootWith(hh);

        // Field 'state_root'
        this.state_root.hashTreeRootWith(hh);

        // Field 'body'
        this.body.hashTreeRootWith(hh);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'slot'
        hh.putUint64(this.slot);

        // Field 'proposer_index'
        hh.putUint64(this.proposer_index);

        // Field 'parent_root'
        this.parent_root.hashTreeRootWith(hh);

        // Field 'state_root'
        this.state_root.hashTreeRootWith(hh);

        // Field 'body'
        this.body.hashTreeRootWith(hh);

        hh.merkleize(idx);
    }
}
