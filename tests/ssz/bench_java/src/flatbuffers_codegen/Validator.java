// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class Validator {
    public byte[] pubkey;
    public byte[] withdrawal_credentials;
    public long effective_balance;
    public boolean slashed;
    public long activation_eligibility_epoch;
    public long activation_epoch;
    public long exit_epoch;
    public long withdrawable_epoch;

    public Validator() {
        this.pubkey = new byte[48];
        this.withdrawal_credentials = new byte[32];
    }

    public int sszBytesLen() {
        return 121;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;
        System.arraycopy(this.pubkey, 0, buf, pos, 48);
        pos += 48;
        System.arraycopy(this.withdrawal_credentials, 0, buf, pos, 32);
        pos += 32;
        bb.putLong(pos, this.effective_balance);
        pos += 8;
        buf[pos] = (byte) (this.slashed ? 1 : 0);
        pos += 1;
        bb.putLong(pos, this.activation_eligibility_epoch);
        pos += 8;
        bb.putLong(pos, this.activation_epoch);
        pos += 8;
        bb.putLong(pos, this.exit_epoch);
        pos += 8;
        bb.putLong(pos, this.withdrawable_epoch);
        pos += 8;
    }

    public static Validator fromSSZBytes(byte[] bytes) {
        if (bytes.length < 121) {
            throw new SszError("buffer too small");
        }

        Validator t = new Validator();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        t.pubkey = new byte[48];
        System.arraycopy(bytes, 0, t.pubkey, 0, 48);
        t.withdrawal_credentials = new byte[32];
        System.arraycopy(bytes, 48, t.withdrawal_credentials, 0, 32);
        t.effective_balance = bb.getLong(80);
        if (bytes[88] > 1) {
            throw new SszError("invalid bool");
        }
        t.slashed = bytes[88] == 1;
        t.activation_eligibility_epoch = bb.getLong(89);
        t.activation_epoch = bb.getLong(97);
        t.exit_epoch = bb.getLong(105);
        t.withdrawable_epoch = bb.getLong(113);
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'pubkey'
        hh.putBytes(this.pubkey);

        // Field 'withdrawal_credentials'
        hh.putBytes(this.withdrawal_credentials);

        // Field 'effective_balance'
        hh.putUint64(this.effective_balance);

        // Field 'slashed'
        hh.putBool(this.slashed);

        // Field 'activation_eligibility_epoch'
        hh.putUint64(this.activation_eligibility_epoch);

        // Field 'activation_epoch'
        hh.putUint64(this.activation_epoch);

        // Field 'exit_epoch'
        hh.putUint64(this.exit_epoch);

        // Field 'withdrawable_epoch'
        hh.putUint64(this.withdrawable_epoch);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'pubkey'
        hh.putBytes(this.pubkey);

        // Field 'withdrawal_credentials'
        hh.putBytes(this.withdrawal_credentials);

        // Field 'effective_balance'
        hh.putUint64(this.effective_balance);

        // Field 'slashed'
        hh.putBool(this.slashed);

        // Field 'activation_eligibility_epoch'
        hh.putUint64(this.activation_eligibility_epoch);

        // Field 'activation_epoch'
        hh.putUint64(this.activation_epoch);

        // Field 'exit_epoch'
        hh.putUint64(this.exit_epoch);

        // Field 'withdrawable_epoch'
        hh.putUint64(this.withdrawable_epoch);

        hh.merkleize(idx);
    }
}
