// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class ProposerSlashing {
    public SignedBeaconBlockHeader signed_header_1;
    public SignedBeaconBlockHeader signed_header_2;

    public ProposerSlashing() {
        this.signed_header_1 = new SignedBeaconBlockHeader();
        this.signed_header_2 = new SignedBeaconBlockHeader();
    }

    public int sszBytesLen() {
        return 416;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;
        this.signed_header_1.marshalSSZTo(buf, pos);
        pos += this.signed_header_1.sszBytesLen();
        this.signed_header_2.marshalSSZTo(buf, pos);
        pos += this.signed_header_2.sszBytesLen();
    }

    public static ProposerSlashing fromSSZBytes(byte[] bytes) {
        if (bytes.length < 416) {
            throw new SszError("buffer too small");
        }

        ProposerSlashing t = new ProposerSlashing();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        {
            byte[] nested = new byte[208];
            System.arraycopy(bytes, 0, nested, 0, 208);
            t.signed_header_1 = SignedBeaconBlockHeader.fromSSZBytes(nested);
        }
        {
            byte[] nested = new byte[208];
            System.arraycopy(bytes, 208, nested, 0, 208);
            t.signed_header_2 = SignedBeaconBlockHeader.fromSSZBytes(nested);
        }
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'signed_header_1'
        this.signed_header_1.hashTreeRootWith(hh);

        // Field 'signed_header_2'
        this.signed_header_2.hashTreeRootWith(hh);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'signed_header_1'
        this.signed_header_1.hashTreeRootWith(hh);

        // Field 'signed_header_2'
        this.signed_header_2.hashTreeRootWith(hh);

        hh.merkleize(idx);
    }
}
