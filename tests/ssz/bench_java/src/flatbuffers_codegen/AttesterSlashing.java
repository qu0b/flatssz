// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class AttesterSlashing {
    public IndexedAttestation attestation_1;
    public IndexedAttestation attestation_2;

    public AttesterSlashing() {
        this.attestation_1 = new IndexedAttestation();
        this.attestation_2 = new IndexedAttestation();
    }

    public int sszBytesLen() {
        int size = 8;
        size += this.attestation_1.sszBytesLen();
        size += this.attestation_2.sszBytesLen();
        return size;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;

        // Offset placeholder for 'attestation_1'
        int offsetPos0 = pos;
        pos += 4;
        // Offset placeholder for 'attestation_2'
        int offsetPos1 = pos;
        pos += 4;

        // Dynamic field 'attestation_1'
        bb.putInt(offsetPos0, pos - offset);
        this.attestation_1.marshalSSZTo(buf, pos);
        pos += this.attestation_1.sszBytesLen();
        // Dynamic field 'attestation_2'
        bb.putInt(offsetPos1, pos - offset);
        this.attestation_2.marshalSSZTo(buf, pos);
        pos += this.attestation_2.sszBytesLen();
    }

    public static AttesterSlashing fromSSZBytes(byte[] bytes) {
        if (bytes.length < 8) {
            throw new SszError("buffer too small");
        }

        AttesterSlashing t = new AttesterSlashing();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        int offset0 = bb.getInt(0);
        if (offset0 != 8) {
            throw new SszError("invalid offset");
        }
        int offset1 = bb.getInt(4);
        if (offset1 < offset0 || offset1 > bytes.length) {
            throw new SszError("invalid offset");
        }

        {
            int sliceStart = offset0;
            int sliceEnd = offset1;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            t.attestation_1 = IndexedAttestation.fromSSZBytes(slice);
        }
        {
            int sliceStart = offset1;
            int sliceEnd = bytes.length;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            t.attestation_2 = IndexedAttestation.fromSSZBytes(slice);
        }
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'attestation_1'
        this.attestation_1.hashTreeRootWith(hh);

        // Field 'attestation_2'
        this.attestation_2.hashTreeRootWith(hh);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'attestation_1'
        this.attestation_1.hashTreeRootWith(hh);

        // Field 'attestation_2'
        this.attestation_2.hashTreeRootWith(hh);

        hh.merkleize(idx);
    }
}
