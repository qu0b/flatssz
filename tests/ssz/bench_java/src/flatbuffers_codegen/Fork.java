// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class Fork {
    public byte[] previous_version;
    public byte[] current_version;
    public long epoch;

    public Fork() {
        this.previous_version = new byte[4];
        this.current_version = new byte[4];
    }

    public int sszBytesLen() {
        return 16;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;
        System.arraycopy(this.previous_version, 0, buf, pos, 4);
        pos += 4;
        System.arraycopy(this.current_version, 0, buf, pos, 4);
        pos += 4;
        bb.putLong(pos, this.epoch);
        pos += 8;
    }

    public static Fork fromSSZBytes(byte[] bytes) {
        if (bytes.length < 16) {
            throw new SszError("buffer too small");
        }

        Fork t = new Fork();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        t.previous_version = new byte[4];
        System.arraycopy(bytes, 0, t.previous_version, 0, 4);
        t.current_version = new byte[4];
        System.arraycopy(bytes, 4, t.current_version, 0, 4);
        t.epoch = bb.getLong(8);
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'previous_version'
        hh.putBytes(this.previous_version);

        // Field 'current_version'
        hh.putBytes(this.current_version);

        // Field 'epoch'
        hh.putUint64(this.epoch);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'previous_version'
        hh.putBytes(this.previous_version);

        // Field 'current_version'
        hh.putBytes(this.current_version);

        // Field 'epoch'
        hh.putUint64(this.epoch);

        hh.merkleize(idx);
    }
}
