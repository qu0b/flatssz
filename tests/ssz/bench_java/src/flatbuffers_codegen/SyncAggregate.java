// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class SyncAggregate {
    public byte[] sync_committee_bits;
    public byte[] sync_committee_signature;

    public SyncAggregate() {
        this.sync_committee_bits = new byte[64];
        this.sync_committee_signature = new byte[96];
    }

    public int sszBytesLen() {
        return 160;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;
        System.arraycopy(this.sync_committee_bits, 0, buf, pos, 64);
        pos += 64;
        System.arraycopy(this.sync_committee_signature, 0, buf, pos, 96);
        pos += 96;
    }

    public static SyncAggregate fromSSZBytes(byte[] bytes) {
        if (bytes.length < 160) {
            throw new SszError("buffer too small");
        }

        SyncAggregate t = new SyncAggregate();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        t.sync_committee_bits = new byte[64];
        System.arraycopy(bytes, 0, t.sync_committee_bits, 0, 64);
        t.sync_committee_signature = new byte[96];
        System.arraycopy(bytes, 64, t.sync_committee_signature, 0, 96);
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'sync_committee_bits'
        hh.putBytes(this.sync_committee_bits);

        // Field 'sync_committee_signature'
        hh.putBytes(this.sync_committee_signature);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'sync_committee_bits'
        hh.putBytes(this.sync_committee_bits);

        // Field 'sync_committee_signature'
        hh.putBytes(this.sync_committee_signature);

        hh.merkleize(idx);
    }
}
