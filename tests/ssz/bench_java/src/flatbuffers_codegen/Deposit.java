// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class Deposit {
    public byte[] proof;
    public DepositData data;

    public Deposit() {
        this.proof = new byte[1056];
        this.data = new DepositData();
    }

    public int sszBytesLen() {
        return 1240;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;
        System.arraycopy(this.proof, 0, buf, pos, 1056);
        pos += 1056;
        this.data.marshalSSZTo(buf, pos);
        pos += this.data.sszBytesLen();
    }

    public static Deposit fromSSZBytes(byte[] bytes) {
        if (bytes.length < 1240) {
            throw new SszError("buffer too small");
        }

        Deposit t = new Deposit();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        t.proof = new byte[1056];
        System.arraycopy(bytes, 0, t.proof, 0, 1056);
        {
            byte[] nested = new byte[184];
            System.arraycopy(bytes, 1056, nested, 0, 184);
            t.data = DepositData.fromSSZBytes(nested);
        }
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'proof'
        hh.putBytes(this.proof);

        // Field 'data'
        this.data.hashTreeRootWith(hh);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'proof'
        hh.putBytes(this.proof);

        // Field 'data'
        this.data.hashTreeRootWith(hh);

        hh.merkleize(idx);
    }
}
