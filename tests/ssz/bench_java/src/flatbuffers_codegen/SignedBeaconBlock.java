// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package flatbuffers_codegen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.util.ArrayList;
import java.util.List;
import ssz.Hasher;
import ssz.SszError;

public class SignedBeaconBlock {
    public BeaconBlock message;
    public Bytes96 signature;

    public SignedBeaconBlock() {
        this.message = new BeaconBlock();
        this.signature = new Bytes96();
    }

    public int sszBytesLen() {
        int size = 100;
        size += this.message.sszBytesLen();
        return size;
    }

    public byte[] marshalSSZ() {
        byte[] buf = new byte[sszBytesLen()];
        marshalSSZTo(buf, 0);
        return buf;
    }

    public void marshalSSZTo(byte[] buf, int offset) {
        ByteBuffer bb = ByteBuffer.wrap(buf);
        bb.order(ByteOrder.LITTLE_ENDIAN);
        int pos = offset;

        // Offset placeholder for 'message'
        int offsetPos0 = pos;
        pos += 4;
        // Field 'signature'
        this.signature.marshalSSZTo(buf, pos);
        pos += this.signature.sszBytesLen();

        // Dynamic field 'message'
        bb.putInt(offsetPos0, pos - offset);
        this.message.marshalSSZTo(buf, pos);
        pos += this.message.sszBytesLen();
    }

    public static SignedBeaconBlock fromSSZBytes(byte[] bytes) {
        if (bytes.length < 100) {
            throw new SszError("buffer too small");
        }

        SignedBeaconBlock t = new SignedBeaconBlock();
        ByteBuffer bb = ByteBuffer.wrap(bytes);
        bb.order(ByteOrder.LITTLE_ENDIAN);

        int offset0 = bb.getInt(0);
        if (offset0 != 100) {
            throw new SszError("invalid offset");
        }
        {
            byte[] nested = new byte[96];
            System.arraycopy(bytes, 4, nested, 0, 96);
            t.signature = Bytes96.fromSSZBytes(nested);
        }

        {
            int sliceStart = offset0;
            int sliceEnd = bytes.length;
            byte[] slice = new byte[sliceEnd - sliceStart];
            System.arraycopy(bytes, sliceStart, slice, 0, sliceEnd - sliceStart);
            t.message = BeaconBlock.fromSSZBytes(slice);
        }
        return t;
    }

    public byte[] hashTreeRoot() {
        Hasher hh = new Hasher();
        int idx = hh.index();

        // Field 'message'
        this.message.hashTreeRootWith(hh);

        // Field 'signature'
        this.signature.hashTreeRootWith(hh);

        hh.merkleize(idx);
        return hh.hashRoot();
    }

    public void hashTreeRootWith(Hasher hh) {
        int idx = hh.index();

        // Field 'message'
        this.message.hashTreeRootWith(hh);

        // Field 'signature'
        this.signature.hashTreeRootWith(hh);

        hh.merkleize(idx);
    }
}
